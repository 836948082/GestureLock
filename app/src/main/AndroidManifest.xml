<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.runtai.gesturelock">

    <uses-permission android:name="android.permission.GET_TASKS" />

    <application
        android:name=".app.MApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activity.WelcomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.DengLuActivity" />

        <activity android:name=".lockpattern_gesture.CreateGestureActivity" />
        <activity android:name=".lockpattern_gesture.GestureLoginActivity" />
        <activity android:name=".lockpattern_gesture.ForgetActivity" />

        <activity android:name=".activity.Activity1" />
        <activity android:name=".activity.Activity2" />
        <activity android:name=".activity.Activity3" />

    </application>

</manifest>